namespace Rainbow.Compilation.Assembler {
    internal enum TokenType {
        TYPE,
        STR,
        INSTR,
        VALUE,
        LBRACKET,
        RBRACKET,
        LPAREN,
        RPAREN,
        CMP,
        LABEL,
    }
}
